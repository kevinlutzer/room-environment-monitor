void loop() {
  // turn the ledPin on
  // read the value from the sensor:

  uint16_t buf[3];
  for (uint8_t i = 0; i < 3; i ++) {
    buf[i] = analogRead(ap[i]);

    Serial.print("Sensor value: ");
    Serial.print(i);
    Serial.print(" ");
    Serial.println(buf[i]);    
  }

  Serial.println("");

  delay(1000);
}
